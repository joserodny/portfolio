let animations = bodymovin.loadAnimation({
    container: document.getElementById('anim'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: '../Animation/drawkit-grape-animation-2-LOOP.json' // the path to the animation json
});